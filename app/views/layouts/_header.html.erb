<nav class="navbar navbar-default">
  <div class="container-fluid">
  <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#login_collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
  </button>
  <div class="navbar-header"> <a class="navbar-brand" href=<%= root_path %>>READSS</a> </div>
  <div class="collapse navbar-collapse" id="login_collapse">
    <p class="navbar-text navbar-right">
        <% if user_signed_in? %>
          <%= current_user.email %>
          <%= current_user.reward %>
          <%= link_to 'Log out', destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to 'Log in', new_user_session_path %>
        <% end %>
    </p>
  </div>
  </div>
</nav>